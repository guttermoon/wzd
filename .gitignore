# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# Photographers' full-resolution originals. Kept out of git — the web and
# press renditions in public/ are generated from them by
# scripts/prepare-images.mjs, and the originals are ~31MB.
/assets/originals/

# The Hitchcock typeface (Matt Terich, after Saul Bass) must not be
# redistributed, so it never enters this repository. It is a design
# reference only — the site ships Grandstander. Free download:
# http://typographica.org/001110.php
*.ttf
Hitchcock*
